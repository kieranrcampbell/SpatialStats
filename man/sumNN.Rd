\name{sumNN}
\alias{sumNN}
\title{Takes a nearest neighbour list (X) and sums the columns
(this assumes they're already properly weighted)}
\usage{
sumNN(X, nprotein)
}
\description{
Takes a nearest neighbour list (X) and sums the columns
(this assumes they're already properly weighted)
}

